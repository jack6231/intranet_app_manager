# Gradle 5.4.1 仅支持 Java 8–12；本机默认 JDK 为 21(Android Studio JBR),无法直接跑 wrapper。
# 指定构建用 JDK 11(brew: openjdk@11),使 `./gradlew` 可在命令行出包。
org.gradle.java.home=/opt/homebrew/opt/openjdk@11/libexec/openjdk.jdk/Contents/Home
